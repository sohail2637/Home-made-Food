import { createAction } from "@reduxjs/toolkit";

export const addtocart = createAction("ADD-TO-CART");
export const sellerpost = createAction('SELLER-POST');
export const loginaction = createAction("USER-LOGIN");
export const shippingdata = createAction("SHIPPING-DATA");
